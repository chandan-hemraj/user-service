---
servicecomb:
  registry:
      address: http://localhost:30100 # If type is File then address will be the path of the file
  protocols:
    rest:
      listenAddress: "0.0.0.0:5001"
  handler:
    chain:
      Provider:
        rest : Payload-Validator